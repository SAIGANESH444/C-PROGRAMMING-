// RANDOM NUMBERS AND DISPLAY NO OF TIMES NO PRESENT

#include<stdio.h>

void main()
{
int i,count=0,sn;
 int arr[10];

 srand(time(0));

   for(i=0;i<=10;i++)
  {
      arr[i] = rand()%100;
      printf("%5d",arr[i]);
  }

       printf("\nenter number");
       scanf("%d",&sn);

      for(i=0;i<=10;i++)
      {
        if(arr[i] == sn)
         count++;
      }
   printf("count = %d",count);
}

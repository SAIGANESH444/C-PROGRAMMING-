// largest string

#include<stdio.h>

void main()
{
int i;
char  st[30],lst[30];

  strcpy(lst,"");

  for(i=1;i <= 5;i++)
  {
  printf("enter string :");
  gets(st);
  if( strcmp(st, lst) >0 )
     strcpy(lst,st);
  }

  printf("Largest string = %s\n",lst);
  }

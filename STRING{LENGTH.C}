// largest string

#include<stdio.h>

void main()
{
int i;
char  st[30],lst[30];

  strcpy(lst,"");

  for(i=1;i <= 5;i++)
  {
  printf("enter string :");
  gets(st);
  if( strlen(st)> strlen(lst))
     strcpy(lst,st);
  }

  printf("Length of string = %s\n",lst);
  }
